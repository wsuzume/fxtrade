# from fractions import Fraction
# from pandas import Timestamp

# class Ticker:
#     def __init__(self, timestamp, best_bid, best_ask, **kwargs):
#         self.timestamp = Timestamp(timestamp)
#         self.best_bid = Fraction(best_bid)
#         self.best_ask = Fraction(best_ask)

#         for k, v in kwargs.items():
#             setattr(self, k, v)